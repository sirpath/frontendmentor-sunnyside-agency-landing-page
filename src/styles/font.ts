import { Barlow, Fraunces } from 'next/font/google'

export const fontBarlow = Barlow({ subsets: ['latin'], weight: ['600'] })
export const fontFraunces = Fraunces({ subsets: ['latin'], weight: ['700', '900'] })
